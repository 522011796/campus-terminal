{
  "name": "campus-terminal",
  "version": "1.0.0",
  "description": "campus terminal",
  "author": "",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": "3000"
    }
  },
  "dependencies": {
    "nuxt": "^2.11.0",
    "cross-env": "^6.0.3",
    "express": "^4.17.1",
    "view-design": "^4.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
